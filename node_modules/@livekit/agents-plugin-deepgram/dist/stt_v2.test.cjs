"use strict";
var import_agents_plugin_silero = require("@livekit/agents-plugin-silero");
var import_agents_plugins_test = require("@livekit/agents-plugins-test");
var import_vitest = require("vitest");
var import_stt_v2 = require("./stt_v2.cjs");
const hasDeepgramApiKey = Boolean(process.env.DEEPGRAM_API_KEY);
if (hasDeepgramApiKey) {
  (0, import_vitest.describe)("Deepgram STTv2 (Flux)", async () => {
    await (0, import_agents_plugins_test.stt)(new import_stt_v2.STTv2(), await import_agents_plugin_silero.VAD.load(), { nonStreaming: false });
  });
} else {
  (0, import_vitest.describe)("Deepgram STTv2 (Flux)", () => {
    import_vitest.it.skip("requires DEEPGRAM_API_KEY", () => {
    });
  });
}
//# sourceMappingURL=stt_v2.test.cjs.map