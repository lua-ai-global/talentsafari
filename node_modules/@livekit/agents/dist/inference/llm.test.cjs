"use strict";
var import_vitest = require("vitest");
var import_llm = require("../llm/index.cjs");
var import_log = require("../log.cjs");
var import_llm2 = require("./llm.cjs");
(0, import_vitest.beforeAll)(() => {
  (0, import_log.initializeLogger)({ level: "silent", pretty: false });
});
async function captureHeaders(opts) {
  const llm = new import_llm2.LLM({
    model: "openai/gpt-4o-mini",
    apiKey: "test-key",
    apiSecret: "test-secret",
    baseURL: "https://example.livekit.cloud",
    inferenceClass: opts.ctor
  });
  let capturedHeaders = {};
  const stub = async (_body, options) => {
    capturedHeaders = (options == null ? void 0 : options.headers) ?? {};
    return {
      [Symbol.asyncIterator]() {
        return {
          next: async () => ({ done: true, value: void 0 })
        };
      }
    };
  };
  const internal = llm;
  internal.client.chat.completions.create = stub;
  const stream = llm.chat({
    chatCtx: new import_llm.ChatContext(),
    inferenceClass: opts.perCall
  });
  for await (const _chunk of stream) {
    void _chunk;
  }
  return capturedHeaders;
}
async function collectChatChunks(completionChunks) {
  const llm = new import_llm2.LLM({
    model: "openai/gpt-4o-mini",
    apiKey: "test-key",
    apiSecret: "test-secret",
    baseURL: "https://example.livekit.cloud"
  });
  const stub = async () => ({
    async *[Symbol.asyncIterator]() {
      for (const chunk of completionChunks) {
        yield chunk;
      }
    }
  });
  const internal = llm;
  internal.client.chat.completions.create = stub;
  const stream = llm.chat({ chatCtx: new import_llm.ChatContext() });
  const chunks = [];
  for await (const chunk of stream) {
    chunks.push(chunk);
  }
  return chunks;
}
(0, import_vitest.describe)("inference.LLM X-LiveKit-Inference-Priority header", () => {
  (0, import_vitest.it)("omits the header when neither constructor nor chat() sets inferenceClass", async () => {
    const headers = await captureHeaders({});
    (0, import_vitest.expect)(headers["X-LiveKit-Inference-Priority"]).toBeUndefined();
  });
  (0, import_vitest.it)("uses constructor 'priority' when chat() does not override", async () => {
    const headers = await captureHeaders({ ctor: "priority" });
    (0, import_vitest.expect)(headers["X-LiveKit-Inference-Priority"]).toBe("priority");
  });
  (0, import_vitest.it)("uses constructor 'standard' when chat() does not override", async () => {
    const headers = await captureHeaders({ ctor: "standard" });
    (0, import_vitest.expect)(headers["X-LiveKit-Inference-Priority"]).toBe("standard");
  });
  (0, import_vitest.it)("uses per-call 'priority' when no constructor default is set", async () => {
    const headers = await captureHeaders({ perCall: "priority" });
    (0, import_vitest.expect)(headers["X-LiveKit-Inference-Priority"]).toBe("priority");
  });
  (0, import_vitest.it)("uses per-call 'standard' when no constructor default is set", async () => {
    const headers = await captureHeaders({ perCall: "standard" });
    (0, import_vitest.expect)(headers["X-LiveKit-Inference-Priority"]).toBe("standard");
  });
  (0, import_vitest.it)("per-call 'standard' overrides constructor 'priority'", async () => {
    const headers = await captureHeaders({ ctor: "priority", perCall: "standard" });
    (0, import_vitest.expect)(headers["X-LiveKit-Inference-Priority"]).toBe("standard");
  });
  (0, import_vitest.it)("per-call 'priority' overrides constructor 'standard'", async () => {
    const headers = await captureHeaders({ ctor: "standard", perCall: "priority" });
    (0, import_vitest.expect)(headers["X-LiveKit-Inference-Priority"]).toBe("priority");
  });
});
(0, import_vitest.describe)("inference.LLM streamed tool calls", () => {
  (0, import_vitest.it)("does not forward assistant content on tool call chunks", async () => {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p;
    const chunks = await collectChatChunks([
      {
        id: "chatcmpl_test",
        choices: [
          {
            index: 0,
            finish_reason: "tool_calls",
            delta: {
              role: "assistant",
              content: 'saveAnswer({"answer":"yes"})',
              tool_calls: [
                {
                  index: 0,
                  id: "call_123",
                  type: "function",
                  function: {
                    name: "saveAnswer",
                    arguments: '{"answer":"yes"}'
                  }
                }
              ]
            }
          }
        ]
      }
    ]);
    (0, import_vitest.expect)(chunks).toHaveLength(1);
    (0, import_vitest.expect)((_b = (_a = chunks[0]) == null ? void 0 : _a.delta) == null ? void 0 : _b.content).toBeUndefined();
    (0, import_vitest.expect)((_d = (_c = chunks[0]) == null ? void 0 : _c.delta) == null ? void 0 : _d.toolCalls).toHaveLength(1);
    (0, import_vitest.expect)((_h = (_g = (_f = (_e = chunks[0]) == null ? void 0 : _e.delta) == null ? void 0 : _f.toolCalls) == null ? void 0 : _g[0]) == null ? void 0 : _h.callId).toBe("call_123");
    (0, import_vitest.expect)((_l = (_k = (_j = (_i = chunks[0]) == null ? void 0 : _i.delta) == null ? void 0 : _j.toolCalls) == null ? void 0 : _k[0]) == null ? void 0 : _l.name).toBe("saveAnswer");
    (0, import_vitest.expect)((_p = (_o = (_n = (_m = chunks[0]) == null ? void 0 : _m.delta) == null ? void 0 : _n.toolCalls) == null ? void 0 : _o[0]) == null ? void 0 : _p.args).toBe('{"answer":"yes"}');
  });
});
//# sourceMappingURL=llm.test.cjs.map