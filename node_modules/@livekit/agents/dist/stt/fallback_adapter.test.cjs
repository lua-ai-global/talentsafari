"use strict";
var import_vitest = require("vitest");
var import_exceptions = require("../_exceptions.cjs");
var import_log = require("../log.cjs");
var import_fallback_adapter = require("./fallback_adapter.cjs");
var import_fake_stt = require("./testing/fake_stt.cjs");
(0, import_vitest.describe)("FallbackAdapter", () => {
  (0, import_vitest.beforeAll)(() => {
    (0, import_log.initializeLogger)({ pretty: false });
    process.on("unhandledRejection", () => {
    });
  });
  (0, import_vitest.it)("throws if no STT instances are provided", () => {
    (0, import_vitest.expect)(() => new import_fallback_adapter.FallbackAdapter({ sttInstances: [] })).toThrow(/at least one STT instance/);
  });
  (0, import_vitest.it)("throws if a non-streaming STT is provided without a VAD", () => {
    const nonStreaming = new import_fake_stt.FakeSTT({
      label: "non-streaming",
      capabilities: { streaming: false, interimResults: false }
    });
    (0, import_vitest.expect)(() => new import_fallback_adapter.FallbackAdapter({ sttInstances: [nonStreaming] })).toThrow(
      /do not support streaming/
    );
  });
  (0, import_vitest.it)("exposes provided instances and telephony-tuned defaults", () => {
    const a = new import_fake_stt.FakeSTT({ label: "a" });
    const b = new import_fake_stt.FakeSTT({ label: "b" });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [a, b] });
    (0, import_vitest.expect)(adapter.sttInstances).toHaveLength(2);
    (0, import_vitest.expect)(adapter.sttInstances[0]).toBe(a);
    (0, import_vitest.expect)(adapter.sttInstances[1]).toBe(b);
    (0, import_vitest.expect)(adapter.maxRetryPerSTT).toBe(1);
    (0, import_vitest.expect)(adapter.attemptTimeoutMs).toBe(1e4);
    (0, import_vitest.expect)(adapter.retryIntervalMs).toBe(5e3);
    (0, import_vitest.expect)(adapter.status.every((s) => s.available)).toBe(true);
  });
  (0, import_vitest.it)("reports streaming=true even when capabilities are mixed (via StreamAdapter wrap)", () => {
    const a = new import_fake_stt.FakeSTT({ label: "a" });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [a] });
    (0, import_vitest.expect)(adapter.capabilities.streaming).toBe(true);
  });
  (0, import_vitest.it)("_recognize falls through to the next instance on error", async () => {
    var _a, _b, _c, _d;
    const primary = new import_fake_stt.FakeSTT({
      label: "primary",
      fakeException: new import_exceptions.APIConnectionError({ message: "primary down" })
    });
    const fallback = new import_fake_stt.FakeSTT({ label: "fallback", fakeTranscript: "hello world" });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [primary, fallback] });
    const result = await adapter.recognize((0, import_fake_stt.emptyAudioFrame)());
    (0, import_vitest.expect)((_b = (_a = result.alternatives) == null ? void 0 : _a[0]) == null ? void 0 : _b.text).toBe("hello world");
    (0, import_vitest.expect)((_c = adapter.status[0]) == null ? void 0 : _c.available).toBe(false);
    (0, import_vitest.expect)((_d = adapter.status[1]) == null ? void 0 : _d.available).toBe(true);
    (0, import_vitest.expect)((await primary.recognizeCh.next()).value).toBeInstanceOf(import_fake_stt.RecognizeSentinel);
    (0, import_vitest.expect)((await fallback.recognizeCh.next()).value).toBeInstanceOf(import_fake_stt.RecognizeSentinel);
  });
  (0, import_vitest.it)("_recognize throws APIConnectionError when every instance fails", async () => {
    var _a, _b;
    const boom = new import_exceptions.APIConnectionError({ message: "down" });
    const a = new import_fake_stt.FakeSTT({ label: "a", fakeException: boom });
    const b = new import_fake_stt.FakeSTT({ label: "b", fakeException: boom });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [a, b] });
    await (0, import_vitest.expect)(adapter.recognize((0, import_fake_stt.emptyAudioFrame)())).rejects.toThrow(/all STTs failed/);
    (0, import_vitest.expect)((_a = adapter.status[0]) == null ? void 0 : _a.available).toBe(false);
    (0, import_vitest.expect)((_b = adapter.status[1]) == null ? void 0 : _b.available).toBe(false);
  });
  (0, import_vitest.it)("_recognize treats non-APIError failures as fallback-worthy too", async () => {
    var _a, _b, _c;
    const a = new import_fake_stt.FakeSTT({ label: "a", fakeException: new Error("anything") });
    const b = new import_fake_stt.FakeSTT({ label: "b", fakeTranscript: "hello world" });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [a, b] });
    const result = await adapter.recognize((0, import_fake_stt.emptyAudioFrame)());
    (0, import_vitest.expect)((_b = (_a = result.alternatives) == null ? void 0 : _a[0]) == null ? void 0 : _b.text).toBe("hello world");
    (0, import_vitest.expect)((_c = adapter.status[0]) == null ? void 0 : _c.available).toBe(false);
  });
  (0, import_vitest.it)("emits 'stt_availability_changed' with { stt, available } when marking unavailable", async () => {
    const primary = new import_fake_stt.FakeSTT({
      label: "primary",
      fakeException: new import_exceptions.APIError("primary down")
    });
    const fallback = new import_fake_stt.FakeSTT({ label: "fallback", fakeTranscript: "hello world" });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [primary, fallback] });
    const handler = import_vitest.vi.fn();
    adapter.on("stt_availability_changed", handler);
    await adapter.recognize((0, import_fake_stt.emptyAudioFrame)());
    (0, import_vitest.expect)(handler).toHaveBeenCalledWith({ stt: primary, available: false });
  });
  (0, import_vitest.it)("recognize recovery probe flips an instance back to available once it succeeds", async () => {
    var _a, _b, _c, _d;
    const primary = new import_fake_stt.FakeSTT({
      label: "primary",
      fakeException: new import_exceptions.APIConnectionError({ message: "primary down" })
    });
    const fallback = new import_fake_stt.FakeSTT({ label: "fallback", fakeTranscript: "hello world" });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [primary, fallback] });
    const availabilityEvents = [];
    adapter.on(
      "stt_availability_changed",
      (ev) => {
        availabilityEvents.push(ev);
      }
    );
    await adapter.recognize((0, import_fake_stt.emptyAudioFrame)());
    (0, import_vitest.expect)((_a = adapter.status[0]) == null ? void 0 : _a.available).toBe(false);
    (0, import_vitest.expect)((_b = adapter.status[1]) == null ? void 0 : _b.available).toBe(true);
    primary.updateOptions({ fakeException: null, fakeTranscript: "recovered" });
    await adapter.recognize((0, import_fake_stt.emptyAudioFrame)());
    const deadline = Date.now() + 500;
    while (Date.now() < deadline && !((_c = adapter.status[0]) == null ? void 0 : _c.available)) {
      await new Promise((r) => setTimeout(r, 5));
    }
    (0, import_vitest.expect)((_d = adapter.status[0]) == null ? void 0 : _d.available).toBe(true);
    (0, import_vitest.expect)(availabilityEvents.map((e) => ({ stt: e.stt.label, available: e.available }))).toEqual([
      { stt: "primary", available: false },
      { stt: "primary", available: true }
    ]);
  });
  (0, import_vitest.it)("recognize emits exactly one metrics_collected event (no double-count)", async () => {
    const primary = new import_fake_stt.FakeSTT({ label: "primary", fakeTranscript: "hello" });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [primary] });
    const received = [];
    adapter.on("metrics_collected", (m) => received.push(m));
    await adapter.recognize((0, import_fake_stt.emptyAudioFrame)());
    (0, import_vitest.expect)(received).toHaveLength(1);
  });
  (0, import_vitest.it)("forwards metrics_collected events from every child instance", () => {
    const a = new import_fake_stt.FakeSTT({ label: "a" });
    const b = new import_fake_stt.FakeSTT({ label: "b" });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [a, b] });
    const received = [];
    adapter.on("metrics_collected", (m) => received.push(m));
    const metric = {
      type: "stt_metrics",
      timestamp: Date.now(),
      requestId: "r",
      durationMs: 10,
      label: a.label,
      audioDurationMs: 500,
      streamed: false
    };
    a.emit("metrics_collected", metric);
    b.emit("metrics_collected", metric);
    (0, import_vitest.expect)(received).toHaveLength(2);
  });
  (0, import_vitest.it)("close detaches the forwarders so orphan events stop flowing through", async () => {
    const a = new import_fake_stt.FakeSTT({ label: "a" });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [a] });
    const received = [];
    adapter.on("metrics_collected", (m) => received.push(m));
    await adapter.close();
    a.emit("metrics_collected", {
      type: "stt_metrics",
      timestamp: Date.now(),
      requestId: "r",
      durationMs: 10,
      label: a.label,
      audioDurationMs: 500,
      streamed: false
    });
    (0, import_vitest.expect)(received).toHaveLength(0);
  });
});
(0, import_vitest.describe)("FallbackSpeechStream (streaming path)", () => {
  (0, import_vitest.beforeAll)(() => {
    (0, import_log.initializeLogger)({ pretty: false });
    process.on("unhandledRejection", () => {
    });
  });
  (0, import_vitest.it)("forwards events from the primary without triggering fallback when it succeeds", async () => {
    var _a, _b;
    const primary = new import_fake_stt.FakeSTT({ label: "primary", fakeTranscript: "hello world" });
    const fallback = new import_fake_stt.FakeSTT({ label: "fallback", fakeTranscript: "hello world" });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [primary, fallback] });
    const availabilityChanges = [];
    adapter.on(
      "stt_availability_changed",
      (ev) => {
        availabilityChanges.push(ev);
      }
    );
    const stream = adapter.stream();
    stream.endInput();
    const events = [];
    for await (const ev of stream) events.push(ev);
    (0, import_vitest.expect)(events.map((e) => {
      var _a2, _b2;
      return (_b2 = (_a2 = e.alternatives) == null ? void 0 : _a2[0]) == null ? void 0 : _b2.text;
    })).toEqual(["hello world"]);
    (0, import_vitest.expect)(availabilityChanges).toEqual([]);
    (0, import_vitest.expect)((_a = adapter.status[0]) == null ? void 0 : _a.available).toBe(true);
    (0, import_vitest.expect)((_b = adapter.status[1]) == null ? void 0 : _b.available).toBe(true);
  });
  (0, import_vitest.it)("stream switches to the secondary provider when the primary errors", async () => {
    var _a, _b;
    const primary = new import_fake_stt.FakeSTT({
      label: "primary",
      fakeException: new import_exceptions.APIError("primary down")
    });
    const fallback = new import_fake_stt.FakeSTT({ label: "fallback", fakeTranscript: "hello world" });
    const adapter = new import_fallback_adapter.FallbackAdapter({
      sttInstances: [primary, fallback],
      maxRetryPerSTT: 0
      // no retries — primary fails once, move on
    });
    const availabilityChanges = [];
    adapter.on(
      "stt_availability_changed",
      (ev) => {
        availabilityChanges.push(ev);
      }
    );
    const stream = adapter.stream();
    stream.endInput();
    const events = [];
    for await (const ev of stream) events.push(ev);
    (0, import_vitest.expect)(events.map((e) => {
      var _a2, _b2;
      return (_b2 = (_a2 = e.alternatives) == null ? void 0 : _a2[0]) == null ? void 0 : _b2.text;
    })).toEqual(["hello world"]);
    (0, import_vitest.expect)(availabilityChanges).toContainEqual({ stt: primary, available: false });
    (0, import_vitest.expect)((_a = adapter.status[0]) == null ? void 0 : _a.available).toBe(false);
    (0, import_vitest.expect)((_b = adapter.status[1]) == null ? void 0 : _b.available).toBe(true);
    (0, import_vitest.expect)((await primary.streamCh.next()).done).toBe(false);
    (0, import_vitest.expect)((await fallback.streamCh.next()).done).toBe(false);
  });
  (0, import_vitest.it)("stream marks every instance unavailable when all children fail", async () => {
    var _a, _b;
    const err = new import_exceptions.APIError("down");
    const a = new import_fake_stt.FakeSTT({ label: "a", fakeException: err });
    const b = new import_fake_stt.FakeSTT({ label: "b", fakeException: err });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [a, b], maxRetryPerSTT: 0 });
    adapter.on("error", () => {
    });
    const stream = adapter.stream();
    stream.endInput();
    const events = [];
    for await (const ev of stream) events.push(ev);
    (0, import_vitest.expect)(events).toEqual([]);
    (0, import_vitest.expect)((_a = adapter.status[0]) == null ? void 0 : _a.available).toBe(false);
    (0, import_vitest.expect)((_b = adapter.status[1]) == null ? void 0 : _b.available).toBe(false);
  });
  (0, import_vitest.it)("ends the fallback child when input EOF arrives before failover", async () => {
    var _a, _b;
    const primary = new import_fake_stt.FakeSTT({
      label: "primary",
      fakeException: new import_exceptions.APIError("primary down")
    });
    const fallback = new import_fake_stt.FakeSTT({ label: "fallback", fakeTranscript: "hello world" });
    const adapter = new import_fallback_adapter.FallbackAdapter({
      sttInstances: [primary, fallback],
      maxRetryPerSTT: 0
    });
    const stream = adapter.stream();
    stream.endInput();
    const events = [];
    const collect = (async () => {
      for await (const ev of stream) events.push(ev);
    })();
    const timeout = new Promise(
      (resolve) => setTimeout(() => resolve("timeout"), 1e3)
    );
    const outcome = await Promise.race([collect.then(() => "ok"), timeout]);
    (0, import_vitest.expect)(outcome).toBe("ok");
    (0, import_vitest.expect)(events.map((e) => {
      var _a2, _b2;
      return (_b2 = (_a2 = e.alternatives) == null ? void 0 : _a2[0]) == null ? void 0 : _b2.text;
    })).toEqual(["hello world"]);
    (0, import_vitest.expect)((_a = adapter.status[0]) == null ? void 0 : _a.available).toBe(false);
    (0, import_vitest.expect)((_b = adapter.status[1]) == null ? void 0 : _b.available).toBe(true);
  });
});
(0, import_vitest.describe)("FallbackAdapter dynamic model/provider getters", () => {
  class IdentifiedFakeSTT extends import_fake_stt.FakeSTT {
    _model;
    _provider;
    constructor(opts) {
      super({ label: opts.label, fakeTranscript: opts.fakeTranscript });
      this._model = opts.model;
      this._provider = opts.provider;
    }
    get model() {
      return this._model;
    }
    get provider() {
      return this._provider;
    }
  }
  (0, import_vitest.it)("returns wrapper defaults before any STT is active", () => {
    const a = new IdentifiedFakeSTT({ label: "a", model: "a-model", provider: "a-provider" });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [a] });
    (0, import_vitest.expect)(adapter.model).toBe("FallbackAdapter");
    (0, import_vitest.expect)(adapter.provider).toBe("livekit");
  });
  (0, import_vitest.it)("reflects the active child after a successful recognize()", async () => {
    const primary = new IdentifiedFakeSTT({
      label: "primary",
      model: "primary-model",
      provider: "primary-provider",
      fakeTranscript: "hello"
    });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [primary] });
    await adapter.recognize((0, import_fake_stt.emptyAudioFrame)());
    (0, import_vitest.expect)(adapter.model).toBe("primary-model");
    (0, import_vitest.expect)(adapter.provider).toBe("primary-provider");
  });
  (0, import_vitest.it)("reflects the fallback child after recognize() falls through", async () => {
    const primary = new IdentifiedFakeSTT({
      label: "primary",
      model: "primary-model",
      provider: "primary-provider"
    });
    primary.updateOptions({ fakeException: new import_exceptions.APIConnectionError({ message: "down" }) });
    const fallback = new IdentifiedFakeSTT({
      label: "fallback",
      model: "fallback-model",
      provider: "fallback-provider",
      fakeTranscript: "hello"
    });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [primary, fallback] });
    await adapter.recognize((0, import_fake_stt.emptyAudioFrame)());
    (0, import_vitest.expect)(adapter.model).toBe("fallback-model");
    (0, import_vitest.expect)(adapter.provider).toBe("fallback-provider");
  });
  (0, import_vitest.it)("reflects the active child once streaming events flow", async () => {
    const primary = new IdentifiedFakeSTT({
      label: "primary",
      model: "primary-model",
      provider: "primary-provider",
      fakeTranscript: "hello world"
    });
    const adapter = new import_fallback_adapter.FallbackAdapter({ sttInstances: [primary] });
    const stream = adapter.stream();
    stream.endInput();
    const events = [];
    for await (const ev of stream) events.push(ev);
    (0, import_vitest.expect)(events.length).toBeGreaterThan(0);
    (0, import_vitest.expect)(adapter.model).toBe("primary-model");
    (0, import_vitest.expect)(adapter.provider).toBe("primary-provider");
  });
});
//# sourceMappingURL=fallback_adapter.test.cjs.map