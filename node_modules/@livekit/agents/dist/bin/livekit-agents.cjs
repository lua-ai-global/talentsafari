#!/usr/bin/env node
"use strict";
var import_commander = require("commander");
var import_download = require("../download.cjs");
var import_version = require("../version.cjs");
const program = new import_commander.Command().name("livekit-agents").description("LiveKit Agents standalone CLI").version(import_version.version);
program.command("download-files").description(
  "Discover installed @livekit/agents-plugin-* packages and run their downloadFiles() without loading your agent code. Intended for Dockerfile layer caching."
).action(async () => {
  const code = await (0, import_download.main)();
  process.exit(code);
});
program.parseAsync(process.argv).catch((error) => {
  console.error(`Fatal error: ${error}`);
  process.exit(1);
});
//# sourceMappingURL=livekit-agents.cjs.map