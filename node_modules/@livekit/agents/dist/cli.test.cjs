"use strict";
var import_vitest = require("vitest");
var import_cli = require("./cli.cjs");
var import_plugin = require("./plugin.cjs");
var import_worker = require("./worker.cjs");
class FailingDownloadPlugin extends import_plugin.Plugin {
  constructor() {
    super({ title: "failing-test-plugin", version: "0.0.0", package: "test-plugin" });
  }
  async downloadFiles() {
    throw new Error("download failed");
  }
}
(0, import_vitest.describe)("download-files CLI", () => {
  const originalArgv = process.argv;
  const originalPlugins = import_plugin.Plugin.registeredPlugins;
  (0, import_vitest.afterEach)(() => {
    process.argv = originalArgv;
    import_plugin.Plugin.registeredPlugins = originalPlugins;
    import_vitest.vi.restoreAllMocks();
  });
  (0, import_vitest.it)("exits non-zero when a plugin fails to download files", async () => {
    process.argv = ["node", "test-agent.js", "download-files"];
    import_plugin.Plugin.registeredPlugins = [new FailingDownloadPlugin()];
    const exitSpy = import_vitest.vi.spyOn(process, "exit").mockImplementation(() => void 0);
    (0, import_cli.runApp)(new import_worker.ServerOptions({ agent: "test-agent.js" }));
    await import_vitest.vi.waitFor(() => {
      (0, import_vitest.expect)(exitSpy).toHaveBeenCalledWith(1);
    });
  });
  (0, import_vitest.it)("includes plugin details in download failure messages", () => {
    const message = (0, import_cli.formatDownloadFailureMessage)([
      { plugin: new FailingDownloadPlugin(), error: new Error("download failed") }
    ]);
    (0, import_vitest.expect)(message).toContain("Failed to download files for 1 plugin");
    (0, import_vitest.expect)(message).toContain("failing-test-plugin");
    (0, import_vitest.expect)(message).toContain("test-plugin@0.0.0");
    (0, import_vitest.expect)(message).toContain("download failed");
  });
});
//# sourceMappingURL=cli.test.cjs.map