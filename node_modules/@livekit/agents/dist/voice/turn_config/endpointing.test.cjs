"use strict";
var import_vitest = require("vitest");
var import_endpointing = require("./endpointing.cjs");
(0, import_vitest.describe)("DynamicEndpointing", () => {
  (0, import_vitest.it)("initializes with configured delays", () => {
    const endpointing = new import_endpointing.DynamicEndpointing({ minDelay: 300, maxDelay: 1e3 });
    (0, import_vitest.expect)(endpointing.minDelay).toBe(300);
    (0, import_vitest.expect)(endpointing.maxDelay).toBe(1e3);
  });
  (0, import_vitest.it)("updates minDelay from pauses between utterances", () => {
    const endpointing = new import_endpointing.DynamicEndpointing({ minDelay: 300, maxDelay: 1e3, alpha: 0.5 });
    endpointing.onEndOfSpeech(1e5);
    endpointing.onStartOfSpeech(100400);
    endpointing.onEndOfSpeech(100500);
    (0, import_vitest.expect)(endpointing.minDelay).toBeCloseTo(350);
  });
  (0, import_vitest.it)("updates maxDelay from pauses before a new turn", () => {
    const endpointing = new import_endpointing.DynamicEndpointing({ minDelay: 300, maxDelay: 1e3, alpha: 0.5 });
    endpointing.onEndOfSpeech(1e5);
    endpointing.onStartOfAgentSpeech(100600);
    endpointing.onStartOfSpeech(101500);
    endpointing.onEndOfSpeech(102e3);
    (0, import_vitest.expect)(endpointing.maxDelay).toBeCloseTo(800);
  });
  (0, import_vitest.it)("skips updates for ignored overlapping speech outside the grace period", () => {
    const endpointing = new import_endpointing.DynamicEndpointing({ minDelay: 300, maxDelay: 1e3, alpha: 0.5 });
    endpointing.onEndOfSpeech(1e5);
    endpointing.onStartOfAgentSpeech(100500);
    endpointing.onStartOfSpeech(101500, true);
    const previousMinDelay = endpointing.minDelay;
    const previousMaxDelay = endpointing.maxDelay;
    endpointing.onEndOfSpeech(101800, true);
    (0, import_vitest.expect)(endpointing.minDelay).toBe(previousMinDelay);
    (0, import_vitest.expect)(endpointing.maxDelay).toBe(previousMaxDelay);
  });
  (0, import_vitest.it)("updates options and clamps learned delays", () => {
    const endpointing = new import_endpointing.DynamicEndpointing({ minDelay: 300, maxDelay: 1e3, alpha: 0.5 });
    endpointing.updateOptions({ minDelay: 500, maxDelay: 2e3 });
    endpointing.onEndOfSpeech(1e5);
    endpointing.onStartOfSpeech(100200);
    (0, import_vitest.expect)(endpointing.minDelay).toBeCloseTo(500);
    endpointing.onEndOfSpeech(101e3);
    endpointing.onStartOfAgentSpeech(102800);
    endpointing.onStartOfSpeech(103500);
    (0, import_vitest.expect)(endpointing.maxDelay).toBeGreaterThan(1e3);
    (0, import_vitest.expect)(endpointing.maxDelay).toBeLessThanOrEqual(2e3);
  });
});
(0, import_vitest.describe)("createEndpointing", () => {
  (0, import_vitest.it)("creates dynamic endpointing for dynamic mode", () => {
    const endpointing = (0, import_endpointing.createEndpointing)({
      mode: "dynamic",
      minDelay: 300,
      maxDelay: 1e3,
      alpha: 0.7
    });
    (0, import_vitest.expect)(endpointing).toBeInstanceOf(import_endpointing.DynamicEndpointing);
  });
  (0, import_vitest.it)("creates base endpointing for fixed mode", () => {
    const endpointing = (0, import_endpointing.createEndpointing)({
      mode: "fixed",
      minDelay: 500,
      maxDelay: 3e3,
      alpha: 0.9
    });
    (0, import_vitest.expect)(endpointing).toBeInstanceOf(import_endpointing.BaseEndpointing);
    (0, import_vitest.expect)(endpointing).not.toBeInstanceOf(import_endpointing.DynamicEndpointing);
    (0, import_vitest.expect)(endpointing.minDelay).toBe(500);
    (0, import_vitest.expect)(endpointing.maxDelay).toBe(3e3);
  });
});
//# sourceMappingURL=endpointing.test.cjs.map