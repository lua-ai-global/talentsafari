"use strict";
var import_vitest = require("vitest");
var import_chat_context = require("../llm/chat_context.cjs");
var import_realtime = require("../llm/realtime.cjs");
var import_log = require("../log.cjs");
var import_agent = require("./agent.cjs");
var import_agent_activity = require("./agent_activity.cjs");
(0, import_log.initializeLogger)({ pretty: false, level: "silent" });
function createFakeActivity(agent, stt) {
  const detachedPipeline = { id: Symbol("pipeline") };
  const activity = {
    agent,
    audioRecognition: {
      detachSttPipeline: import_vitest.vi.fn(async () => detachedPipeline)
    },
    stt,
    llm: void 0,
    tools: [],
    realtimeSession: void 0
  };
  return { activity, detachedPipeline };
}
async function detachResources(oldActivity, newActivity) {
  const fn = import_agent_activity.AgentActivity.prototype._detachReusableResources;
  return await fn.call(oldActivity, newActivity);
}
(0, import_vitest.describe)("AgentActivity STT handoff reuse eligibility", () => {
  (0, import_vitest.it)("reuses the pipeline when both activities share the same STT instance and sttNode", async () => {
    var _a;
    const sharedStt = { id: "shared-stt" };
    const oldActivity = createFakeActivity(new import_agent.Agent({ instructions: "a" }), sharedStt);
    const newActivity = createFakeActivity(new import_agent.Agent({ instructions: "b" }), sharedStt);
    const resources = await detachResources(oldActivity.activity, newActivity.activity);
    (0, import_vitest.expect)(resources.sttPipeline).toBe(oldActivity.detachedPipeline);
    (0, import_vitest.expect)((_a = oldActivity.activity.audioRecognition) == null ? void 0 : _a.detachSttPipeline).toHaveBeenCalledTimes(1);
  });
  (0, import_vitest.it)("does not reuse when the STT instances differ", async () => {
    var _a;
    const oldActivity = createFakeActivity(new import_agent.Agent({ instructions: "a" }), { id: "stt-a" });
    const newActivity = createFakeActivity(new import_agent.Agent({ instructions: "b" }), { id: "stt-b" });
    const resources = await detachResources(oldActivity.activity, newActivity.activity);
    (0, import_vitest.expect)(resources.sttPipeline).toBeUndefined();
    (0, import_vitest.expect)((_a = oldActivity.activity.audioRecognition) == null ? void 0 : _a.detachSttPipeline).not.toHaveBeenCalled();
  });
  (0, import_vitest.it)("does not reuse when either activity has no STT", async () => {
    var _a;
    const sharedStt = { id: "shared-stt" };
    const oldActivity = createFakeActivity(new import_agent.Agent({ instructions: "a" }), void 0);
    const newActivity = createFakeActivity(new import_agent.Agent({ instructions: "b" }), sharedStt);
    const resources = await detachResources(oldActivity.activity, newActivity.activity);
    (0, import_vitest.expect)(resources.sttPipeline).toBeUndefined();
    (0, import_vitest.expect)((_a = oldActivity.activity.audioRecognition) == null ? void 0 : _a.detachSttPipeline).not.toHaveBeenCalled();
  });
  (0, import_vitest.it)("does not reuse when the agents override sttNode differently", async () => {
    var _a;
    const sharedStt = { id: "shared-stt" };
    class AgentA extends import_agent.Agent {
      async sttNode(_audio, _modelSettings) {
        return null;
      }
    }
    class AgentB extends import_agent.Agent {
      async sttNode(_audio, _modelSettings) {
        return null;
      }
    }
    const oldActivity = createFakeActivity(new AgentA({ instructions: "a" }), sharedStt);
    const newActivity = createFakeActivity(new AgentB({ instructions: "b" }), sharedStt);
    const resources = await detachResources(oldActivity.activity, newActivity.activity);
    (0, import_vitest.expect)(resources.sttPipeline).toBeUndefined();
    (0, import_vitest.expect)((_a = oldActivity.activity.audioRecognition) == null ? void 0 : _a.detachSttPipeline).not.toHaveBeenCalled();
  });
  (0, import_vitest.it)("reuses when the new agent inherits the same sttNode implementation", async () => {
    var _a;
    const sharedStt = { id: "shared-stt" };
    class AgentA extends import_agent.Agent {
      async sttNode(_audio, _modelSettings) {
        return null;
      }
    }
    class AgentB extends AgentA {
    }
    const oldActivity = createFakeActivity(new AgentA({ instructions: "a" }), sharedStt);
    const newActivity = createFakeActivity(new AgentB({ instructions: "b" }), sharedStt);
    const resources = await detachResources(oldActivity.activity, newActivity.activity);
    (0, import_vitest.expect)(resources.sttPipeline).toBe(oldActivity.detachedPipeline);
    (0, import_vitest.expect)((_a = oldActivity.activity.audioRecognition) == null ? void 0 : _a.detachSttPipeline).toHaveBeenCalledTimes(1);
  });
  (0, import_vitest.it)("does not reuse when the old activity has no audioRecognition", async () => {
    const sharedStt = { id: "shared-stt" };
    const oldActivity = createFakeActivity(new import_agent.Agent({ instructions: "a" }), sharedStt);
    const newActivity = createFakeActivity(new import_agent.Agent({ instructions: "b" }), sharedStt);
    oldActivity.activity.audioRecognition = void 0;
    const resources = await detachResources(oldActivity.activity, newActivity.activity);
    (0, import_vitest.expect)(resources.sttPipeline).toBeUndefined();
  });
});
(0, import_vitest.describe)("AgentActivity RT session reuse eligibility", () => {
  function createFakeRtSession() {
    return {
      chatCtx: import_chat_context.ChatContext.empty(),
      off: import_vitest.vi.fn(),
      on: import_vitest.vi.fn(),
      interrupt: import_vitest.vi.fn(),
      clearAudio: import_vitest.vi.fn(),
      close: import_vitest.vi.fn(async () => {
      })
    };
  }
  class FakeRealtimeModel extends import_realtime.RealtimeModel {
    get model() {
      return "fake";
    }
    label() {
      return "fake.RealtimeModel";
    }
    session() {
      throw new Error("not implemented");
    }
    async close() {
    }
  }
  function createFakeRealtimeModel(capabilitiesOverrides = {}) {
    const capabilities = {
      messageTruncation: false,
      turnDetection: false,
      userTranscription: false,
      autoToolReplyGeneration: false,
      audioOutput: true,
      manualFunctionCalls: false,
      midSessionChatCtxUpdate: false,
      midSessionInstructionsUpdate: false,
      midSessionToolsUpdate: false,
      ...capabilitiesOverrides
    };
    return new FakeRealtimeModel(capabilities);
  }
  function createRtActivity(agent, llm, rtSession) {
    return {
      agent,
      audioRecognition: void 0,
      stt: void 0,
      llm,
      tools: agent.toolCtx,
      realtimeSession: rtSession
    };
  }
  (0, import_vitest.it)("reuses RT session when same LLM, same instructions, equivalent context, and same tools", async () => {
    const sharedLlm = createFakeRealtimeModel();
    const rtSession = createFakeRtSession();
    const oldAgent = new import_agent.Agent({ instructions: "hello" });
    const newAgent = new import_agent.Agent({ instructions: "hello" });
    const oldActivity = createRtActivity(oldAgent, sharedLlm, rtSession);
    const newActivity = createRtActivity(newAgent, sharedLlm);
    const resources = await detachResources(oldActivity, newActivity);
    (0, import_vitest.expect)(resources.rtSession).toBe(rtSession);
    (0, import_vitest.expect)(oldActivity.realtimeSession).toBeUndefined();
    (0, import_vitest.expect)(rtSession.off).toHaveBeenCalled();
  });
  (0, import_vitest.it)("does not reuse RT session when LLM instances differ", async () => {
    const rtSession = createFakeRtSession();
    const oldLlm = createFakeRealtimeModel();
    const newLlm = createFakeRealtimeModel();
    const oldActivity = createRtActivity(new import_agent.Agent({ instructions: "a" }), oldLlm, rtSession);
    const newActivity = createRtActivity(new import_agent.Agent({ instructions: "a" }), newLlm);
    const resources = await detachResources(oldActivity, newActivity);
    (0, import_vitest.expect)(resources.rtSession).toBeUndefined();
  });
  (0, import_vitest.it)("does not reuse RT session when instructions differ and midSessionInstructionsUpdate is false", async () => {
    const sharedLlm = createFakeRealtimeModel({ midSessionInstructionsUpdate: false });
    const rtSession = createFakeRtSession();
    const oldActivity = createRtActivity(new import_agent.Agent({ instructions: "old" }), sharedLlm, rtSession);
    const newActivity = createRtActivity(new import_agent.Agent({ instructions: "new" }), sharedLlm);
    const resources = await detachResources(oldActivity, newActivity);
    (0, import_vitest.expect)(resources.rtSession).toBeUndefined();
  });
  (0, import_vitest.it)("reuses RT session when instructions differ but midSessionInstructionsUpdate is true", async () => {
    const sharedLlm = createFakeRealtimeModel({ midSessionInstructionsUpdate: true });
    const rtSession = createFakeRtSession();
    const oldActivity = createRtActivity(new import_agent.Agent({ instructions: "old" }), sharedLlm, rtSession);
    const newActivity = createRtActivity(new import_agent.Agent({ instructions: "new" }), sharedLlm);
    const resources = await detachResources(oldActivity, newActivity);
    (0, import_vitest.expect)(resources.rtSession).toBe(rtSession);
  });
  (0, import_vitest.it)("reuses RT session when context differs but midSessionChatCtxUpdate is true", async () => {
    const sharedLlm = createFakeRealtimeModel({ midSessionChatCtxUpdate: true });
    const rtSession = createFakeRtSession();
    const oldActivity = createRtActivity(new import_agent.Agent({ instructions: "same" }), sharedLlm, rtSession);
    const newActivity = createRtActivity(new import_agent.Agent({ instructions: "same" }), sharedLlm);
    const resources = await detachResources(oldActivity, newActivity);
    (0, import_vitest.expect)(resources.rtSession).toBe(rtSession);
  });
  (0, import_vitest.it)("does not reuse when no RT session exists", async () => {
    const sharedLlm = createFakeRealtimeModel();
    const oldActivity = createRtActivity(new import_agent.Agent({ instructions: "a" }), sharedLlm, void 0);
    const newActivity = createRtActivity(new import_agent.Agent({ instructions: "a" }), sharedLlm);
    const resources = await detachResources(oldActivity, newActivity);
    (0, import_vitest.expect)(resources.rtSession).toBeUndefined();
  });
  (0, import_vitest.it)("does not reuse when LLM is not a RealtimeModel", async () => {
    const rtSession = createFakeRtSession();
    const nonRealtimeLlm = { id: "plain-llm" };
    const oldActivity = createRtActivity(
      new import_agent.Agent({ instructions: "a" }),
      nonRealtimeLlm,
      rtSession
    );
    const newActivity = createRtActivity(new import_agent.Agent({ instructions: "a" }), nonRealtimeLlm);
    const resources = await detachResources(oldActivity, newActivity);
    (0, import_vitest.expect)(resources.rtSession).toBeUndefined();
  });
});
(0, import_vitest.describe)("cleanupReusableResources", () => {
  (0, import_vitest.it)("closes both STT pipeline and RT session", async () => {
    const sttClose = import_vitest.vi.fn(async () => {
    });
    const rtClose = import_vitest.vi.fn(async () => {
    });
    const resources = {
      sttPipeline: { close: sttClose },
      rtSession: { close: rtClose }
    };
    await (0, import_agent_activity.cleanupReusableResources)(resources, (0, import_log.log)());
    (0, import_vitest.expect)(sttClose).toHaveBeenCalledTimes(1);
    (0, import_vitest.expect)(rtClose).toHaveBeenCalledTimes(1);
    (0, import_vitest.expect)(resources.sttPipeline).toBeUndefined();
    (0, import_vitest.expect)(resources.rtSession).toBeUndefined();
  });
  (0, import_vitest.it)("handles partial resources (only STT)", async () => {
    const sttClose = import_vitest.vi.fn(async () => {
    });
    const resources = {
      sttPipeline: { close: sttClose }
    };
    await (0, import_agent_activity.cleanupReusableResources)(resources, (0, import_log.log)());
    (0, import_vitest.expect)(sttClose).toHaveBeenCalledTimes(1);
    (0, import_vitest.expect)(resources.sttPipeline).toBeUndefined();
  });
  (0, import_vitest.it)("handles empty resources", async () => {
    const resources = {};
    await (0, import_agent_activity.cleanupReusableResources)(resources, (0, import_log.log)());
  });
});
//# sourceMappingURL=agent_activity_handoff.test.cjs.map