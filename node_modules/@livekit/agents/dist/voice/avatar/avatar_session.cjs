"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var avatar_session_exports = {};
__export(avatar_session_exports, {
  AvatarSession: () => AvatarSession
});
module.exports = __toCommonJS(avatar_session_exports);
var import_job = require("../../job.cjs");
var import_log = require("../../log.cjs");
class AvatarSession {
  #logger = (0, import_log.log)();
  /**
   * Start the avatar session.
   *
   * Subclasses should override this method and call `super.start(agentSession, room)` at the
   * top of their implementation. Subclasses may widen the return type (e.g. returning a
   * session id), matching the `# type: ignore[override]` escape hatch used in Python.
   */
  async start(agentSession, _room) {
    const jobCtx = (0, import_job.getJobContext)(false);
    if (jobCtx !== void 0) {
      jobCtx.addShutdownCallback(() => this.aclose());
    } else {
      this.#logger.debug(
        "AvatarSession started outside a job context; call aclose() manually to release resources when the job shuts down"
      );
    }
    const audioOutput = agentSession.output.audio;
    if (agentSession._started && audioOutput !== null) {
      this.#logger.warn(
        { audioOutput: audioOutput.constructor.name },
        "AvatarSession.start() was called after AgentSession.start(); the existing audio output may be replaced by the avatar. Please start the avatar session before AgentSession.start() to avoid this."
      );
    }
    return void 0;
  }
  /**
   * Release any resources owned by this avatar session. Default implementation is a no-op;
   * subclasses can override to perform cleanup.
   */
  async aclose() {
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  AvatarSession
});
//# sourceMappingURL=avatar_session.cjs.map