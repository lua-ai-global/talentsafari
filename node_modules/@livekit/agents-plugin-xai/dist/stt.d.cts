import { type APIConnectOptions, type AudioBuffer, stt } from '@livekit/agents';
export type STTLanguages = 'ar' | 'cs' | 'da' | 'nl' | 'en' | 'fil' | 'fr' | 'de' | 'hi' | 'id' | 'it' | 'ja' | 'ko' | 'mk' | 'ms' | 'fa' | 'pl' | 'pt' | 'ro' | 'ru' | 'es' | 'sv' | 'th' | 'tr' | 'vi';
export interface STTOptions {
    apiKey?: string;
    interimResults: boolean;
    sampleRate: number;
    enableDiarization: boolean;
    language: STTLanguages | string;
    /**
     * Silence duration in milliseconds before an utterance-final event is fired.
     * xAI's default is 10ms, but we default to 100ms for better compatibility with LK EOT models.
     */
    endpointing: number;
}
export declare class STT extends stt.STT {
    #private;
    label: string;
    get provider(): string;
    constructor(opts?: Partial<STTOptions>);
    _recognize(buffer: AudioBuffer): Promise<stt.SpeechEvent>;
    stream(options?: {
        connOptions?: APIConnectOptions;
    }): SpeechStream;
    updateOptions(opts: Partial<STTOptions>): void;
    close(): Promise<void>;
}
export declare class SpeechStream extends stt.SpeechStream {
    #private;
    label: string;
    constructor(stt: STT, opts: STTOptions, apiKey: string, connOptions?: APIConnectOptions);
    updateOptions(opts: Partial<STTOptions>): void;
    protected run(): Promise<void>;
}
//# sourceMappingURL=stt.d.ts.map