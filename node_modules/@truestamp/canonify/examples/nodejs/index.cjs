// A simple Node.js example utilizing the @truestamp/canonify client.

// Usage:
//   pnpm build (in the root of the repository)
//   node examples/nodejs/index.js

// Load from local lib in development
// eslint-disable-next-line @typescript-eslint/no-var-requires
const { canonify } = require('../../dist/index.js')

// Load from npm
// const { canonify } = require('@truestamp/canonify')

const example = {
  big: BigInt(42).toString(),
  f: false,
  fun: () => {
    console.log('hello')
  },
  n: null,
  num: 42,
  s: 'string',
  sym: Symbol('hello'),
  t: true,
  u: undefined,
  a: [
    undefined,
    null,
    true,
    false,
    'foo',
    42,
    BigInt(42).toString(),
    Symbol('hello'),
    () => {
      console.log('hello')
    },
  ],
}

console.log(canonify(example))
